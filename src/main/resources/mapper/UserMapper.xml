<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tencent.wxcloudrun.dao.UserMapper">

    <resultMap id="userWithRolesResultMap" type="com.tencent.wxcloudrun.dto.UserWithRolesDTO">
        <id property="id" column="id"/>
        <result property="openid" column="openid"/>
        <result property="username" column="username"/>
        <result property="avatar" column="avatar"/>
        <result property="createTime" column="create_time"/>

        <collection property="roles" ofType="com.tencent.wxcloudrun.model.Roles"
                    select="selectRolesByUserId" column="id"/>
    </resultMap>

    <select id="selectAllUsersWithRoles" resultMap="userWithRolesResultMap">
        SELECT id,openid, username, avatar, create_time
        FROM user
        ORDER BY create_time DESC
    </select>

    <select id="selectRolesByUserId" resultType="com.tencent.wxcloudrun.model.Roles">
        SELECT r.id, r.name, r.description,r.created_at,r.updated_at
        FROM roles r
        INNER JOIN user_roles ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

</mapper>